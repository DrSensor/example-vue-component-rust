[package]
name = "algebra-matrix2x2"
version = "0.1.0"
authors = ["Fahmi Akbar Wildana <f.a.wildana@gmail.com>"]

[lib]
crate-type = ["cdylib"]
path = "calculator.rs"

[dependencies]
nalgebra = { version = "0.15", default-features = false, features = [ "alloc" ] }